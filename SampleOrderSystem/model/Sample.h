#pragma once

// TODO(Phase 2): Sample 클래스 - 시료 ID, 이름, 평균 생산시간, 수율(정상 생산 시료 수 / 총 생산 시료 수), 재고 수량
// 참고: CLAUDE.md "도메인 모델 및 상태 흐름 > 시료(Sample)"
// - 등록된 시료만 주문 가능
// - 실 생산량 = ceil(부족분 / 수율), 총 생산 시간 = 평균 생산시간 * 실 생산량 계산에 필요한 값들을 보유한다
//   (계산 로직 자체는 produce_agent 책임이며, 이 클래스는 값만 제공한다)
// - 재고 증감/조회 등 저장소 역할은 이후 Phase에서 함께 정의한다

class Sample
{
    // TODO(Phase 2): 필드/메서드 구현 예정
};
